<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.northstar.bi.dao.CategoryDao">
	<select id="getCategoryByCategoryNo" parameterType="int" resultType="Category">
		select
			CATE_NO,
			CATE_DIVISION_NAME,
			CATE_DIVISION_LEVEL,
			CATE_SECTION_NAME,
			CATE_SECTION_LEVEL,
			CATE_VALUE,
			CATE_AUTH_NO
		from
			bi_category
		where
			CATE_NO = #{value}
	</select>
	<select id="getCateSectionListByDivisionName" parameterType="String" resultType="Category">
		select 
			CATE_NO,
			CATE_DIVISION_NAME,
			CATE_DIVISION_LEVEL,
			CATE_SECTION_NAME,
			CATE_SECTION_LEVEL,
			CATE_VALUE,
			CATE_AUTH_NO
		from
			bi_category
		where
			CATE_DIVISION_NAME = #{value}
		order by
			CATE_SECTION_LEVEL, CATE_SECTION_LEVEL
	</select>
	<select id="getAllCategoryList" resultType="Category">
		select 
			CATE_NO,
			CATE_DIVISION_NAME,
			CATE_DIVISION_LEVEL,
			CATE_SECTION_NAME,
			CATE_SECTION_LEVEL,
			CATE_VALUE,
			CATE_AUTH_NO
		from
			bi_category
		order by
			CATE_DIVISION_LEVEL , CATE_SECTION_LEVEL
	</select>
	<select id="getCategoryList" resultType="Category">
		select 
			CATE_NO,
			CATE_DIVISION_NAME,
			CATE_DIVISION_LEVEL,
			CATE_SECTION_NAME,
			CATE_SECTION_LEVEL,
			CATE_VALUE,
			CATE_AUTH_NO
		from
			bi_category
		where
			CATE_SECTION_LEVEL = 1
		order by
			CATE_DIVISION_LEVEL , CATE_SECTION_LEVEL
	</select>
</mapper>